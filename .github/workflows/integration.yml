###############################################################################
#
# Copyright (C) 2022-2023 Maxim Integrated Products, Inc. (now owned by
# Analog Devices, Inc.),
# Copyright (C) 2023-2024 Analog Devices, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
##############################################################################
name: Integration

concurrency:
  group: ${{ github.workflow }}-&{{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:
env:
  BOARD: max32655_board_3

jobs:
  run_integration:
    runs-on: [self-hosted, btm-ci]
    if: github.event.pull_request.draft == false
    steps:
      - name: envsetup
        run: |
          resource_manager -v
          printenv
          FULL_RESULTS_DIR=${GITHUB_WORKSPACE}/$RESULTS_DIR
          echo "FULL_RESULTS_DIR=${GITHUB_WORKSPACE}/${RESULTS_DIR}" >> $GITHUB_ENV
          mkdir -p $FULL_RESULTS_DIR

      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          submodules: false
          repository: analogdevicesinc/msdk
          ref: fix/me18_gcr
          fetch-depth: 0
          path: msdk

      - name: Lock Resources
        id: lock
        uses: Analog-Devices-MSDK/btm-ci-scripts/actions/lock-board@v1.1
        with:
          boards: |
            ${{env.BOARD}}

          lock: true
      - name: DEBUG
        run: |
          EXAMPLE=msdk/Examples/MAX32655/Bluetooth/BLE5_ctr

          make -C $EXAMPLE
          ocdflash $BOARD $EXAMPLE/build/max32655.elf $GITHUB_REF
      # - name: Flash DUTs
      #   id: flash
      #   uses: Analog-Devices-MSDK/btm-ci-scripts/actions/ocdflash@v1.1
      #   with:
      #     board: |
      #       ${{env.BOARD}}
      #     msdk_path: msdk
      #     project: BLE5_ctr
      #     build_flags: DEBUG=1

      - name: Test
        run: |

          HCI=$(resource_manager -g $BOARD.hci_port)
          ./install.sh
          # sed -i 's/PORT = ".*"/PORT = "$HCI"/' tests/test.py 
          # (cd tests && python3 test.py)

      - name: Unlock Resources
        if: always()
        uses: Analog-Devices-MSDK/btm-ci-scripts/actions/lock-board@v1.1
        with:
          all_owned: true
