import argparse






def main():
    parser = argparse.ArgumentParser(description='HCI test bench CLI')
    parser.add_argument('serial_port', help='Serial port of connected controller')
    parser.add_argument('-f', '--file', )
